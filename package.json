{
  "name": "portfolio-gallery",
  "version": "1.0.0",
  "description": "Portfolio Gallery Application",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "cd backend && npm start",
    "heroku-postbuild": "cd backend && npm install && cd ../frontend && npm install --production=false && npm run build",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\""
  },
  "keywords": [
    "portfolio",
    "gallery",
    "mern"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 